{
    "name": "drxian/wp-omni-auth",
    "description": "OAuth login for WordPress with GitHub, Google, and custom providers.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "10up/wp_mock": "^1.0"
    },
    "config": {
        "allow-plugins": {}
    },
    "scripts": {
        "test": "phpunit"
    }
}
